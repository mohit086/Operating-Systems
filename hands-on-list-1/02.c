#include<stdio.h>

/*
2. Background Process Exploration: Develop a simple program to execute indefinitely in the
background. Traverse the /proc directory and extract relevant process information from the
corresponding proc directories.
*/

int main(){
	while (1);
}

/*./a.out, then cd/proc/{pid} from another terminal*/